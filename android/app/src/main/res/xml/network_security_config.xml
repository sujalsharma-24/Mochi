<?xml version="1.0" encoding="utf-8"?>
<!--
  Android blocks cleartext (plain HTTP) traffic by default since API 28 - the Firebase Auth/
  Firestore SDKs' useEmulator()/setSslEnabled(false) calls in MochiApplication.kt need this
  exception to reach the Firebase Local Emulator Suite over plain HTTP. Only loopback addresses
  are allowlisted (127.0.0.1 for the physical-phone `adb reverse` workflow, 10.0.2.2 for an AVD
  emulator - see android/README.md) - harmless in a release build since neither is ever reachable
  outside local dev.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
